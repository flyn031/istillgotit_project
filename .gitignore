# Django specific ignores
__pycache__/
*.py[co]
*/migrations/*.py # Ignore new, unapplied migrations (optional, but keeps repository cleaner)
*.sqlite3 # If you're using the default SQLite database file

# Environment
.env # Ignore files with environment variables
venv/ # Ignore the virtual environment directory
ENV/
env/

# IDEs
.vscode/ # VS Code settings (optional)
.idea/ # PyCharm settings (optional)

# Generated files
staticfiles/ # Collected static files for deployment
media/ # User-uploaded media files

# Sensitive files (if not using .env)
# config/settings.py # ONLY uncomment if you move SECRET_KEY etc. to env vars